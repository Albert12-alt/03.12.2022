class Pensil():
   def __init__(self, name, size, color):
        self.name = name
        self.size = 13 
        self.color = color 

def write(self):
        print(f"{self.name} пишет")

class PensilWitRubber():
    def __init__(self, name, size, color):
        self.name = name
        self.size = 14
        self.color = color

def write(self):
        print(f"{self.name} пишет")

def washen(self):
        print(f"{self.name} стирает написсаное")


class PenWitRubber():
    def __init__(self, name, size, color):
        self.name = name
        self.size = 14
        self.color = color

def write(self):
        print(f"{self.name} пишет")

def washen(self):
        print(f"{self.name} стирает написсаное")

def   ElectricShock(self, target):
         print(f"{self.name} бьет током")
         target.hp -= 10
